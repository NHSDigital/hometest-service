---
repos:
  # https://github.com/pre-commit/pre-commit-hooks/releases
  - repo: https://github.com/pre-commit/pre-commit-hooks
    rev: v6.0.0
    hooks:
      - id: trailing-whitespace
        args: [--markdown-linebreak-ext=md]
      - id: end-of-file-fixer
      - id: check-yaml
      - id: check-json
      - id: check-added-large-files
      - id: check-case-conflict
      - id: check-merge-conflict
      - id: detect-private-key
      - id: check-executables-have-shebangs
      - id: forbid-submodules

  - repo: local
    hooks:
      - id: trivy-fs-scan
        name: Trivy filesystem scan
        entry: trivy fs --scanners vuln --severity HIGH,CRITICAL --exit-code 1 .
        language: system
        pass_filenames: false

  # - repo: https://github.com/mxab/pre-commit-trivy.git
  #   rev: v0.17.0
  #   hooks:
  #     - id: trivyfs-docker
  #       args:
  #         - --skip-dirs
  #         - ./tests

  - repo: https://github.com/adrienverge/yamllint
    rev: v1.38.0
    hooks:
      - id: yamllint
        name: yamllint
        description: This hook runs yamllint.
        entry: yamllint
        language: python
        types: [file, yaml]

  - repo: https://github.com/zricethezav/gitleaks
    rev: v8.30.0
    hooks:
      - id: gitleaks

  # Markdown linting
  - repo: https://github.com/igorshubovych/markdownlint-cli
    rev: v0.47.0
    hooks:
      - id: markdownlint
        # exclude: ^infrastructure/src
        args: [--fix]

  - repo: https://github.com/antonbabenko/pre-commit-terraform
    rev: v1.105.0
    hooks:
      - id: terraform_fmt
        files: ^local-environment/infra
        args:
          - --args=-recursive

    # - repo: https://github.com/jumanjihouse/pre-commit-hook-yamlfmt
    #   rev: 0.2.3 # or other specific tag
    #   files: ^architecture/api_spec.yaml
    #   hooks:
    #       - id: yamlfmt

    # - repo: local
    #   hooks:
    #     # Format code (fast, staged files only)
    #     - id: prettier
    #       name: prettier
    #       language: node
    #       entry: npx prettier --write
    #       files: "\\.(ts|tsx|js|jsx|json|ya?ml|md)$"
    #       types_or: [file]
    #       pass_filenames: true

    #     # Lint staged TS/JS
    #     - id: eslint
    #       name: eslint
    #       language: node
    #       entry: npx eslint --max-warnings=0
    #       files: "\\.(ts|tsx|js|jsx)$"
    #       pass_filenames: true

    #     # Type-check staged TS files only (fast incremental)
    #     # requires `tsc-files` dev dependency
    #     - id: typescript-staged-check
    #       name: TypeScript staged type-check
    #       language: node
    #       entry: npx tsc-files --noEmit
    #       files: "\\.(ts|tsx)$"
    #       pass_filenames: true

    #     # Quick unit tests for changed files / critical packages
    #     # assume you have a jest config and use testMatch by filename pattern
    #     - id: jest-quick
    #       name: Jest quick tests on staged files
    #       language: system
    #       entry: bash
    #       pass_filenames: true
    #       args:
    #         - -c
    #         - |
    #           STAGED_TESTS=$(printf '%s\n' "$@" | grep -E '\.test\.ts$' || true)
    #           if [ -n "$STAGED_TESTS" ]; then
    #             npx jest --runInBand --findRelatedTests $STAGED_TESTS
    #           fi

    #     # -----------------------
    #     # Security / secrets / basic SAST
    #     # -----------------------

    #     # Node.js dependency audit (fast, local)
    #     # use `--production=false` or `--omit=dev` depending on your policy
    #     - id: npm-audit
    #       name: npm audit (quick)
    #       language: system
    #       entry: bash
    #       pass_filenames: false
    #       args:
    #         - -c
    #         - |
    #           if command -v npm >/dev/null 2>&1; then
    #             npm audit --audit-level=high --omit=dev || exit 1
    #           fi
